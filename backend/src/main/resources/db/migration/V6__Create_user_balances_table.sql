CREATE TABLE user_balances (user_id UUID NOT NULL PRIMARY KEY, balance DECIMAL(15,2) NOT NULL DEFAULT 0.00, updated_at TIMESTAMP WITHOUT TIME ZONE NOT NULL DEFAULT now(), CONSTRAINT fk_user_balances_user FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE); 
